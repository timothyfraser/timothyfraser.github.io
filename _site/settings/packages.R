# packages.R

# Install these packages to run related files in this repository
install.packages(
  c("tidyverse",  "sf",
    "fontawesome", "magick", "viridis", "ggtext", "ggpubr", 
    "flexdashboard", "rmdformats", "blogdown",
    "credentials", "gert")
)
